syntax = "proto3";

// TODO: this is probably temporary should we use the gateway protocols here?
message data_credits_payment_req {
    bytes payee = 1;
    uint64 amount = 2;
}

message data_credits_payment {
    bytes key = 1;
    bytes payer = 2;
    bytes payee = 3;
    uint64 amount = 4;
    bytes signature = 5;
}